import { renderWithProviders } from './test-util-react';

describe('renderWithProviders', () => {
  it('should export renderWithProviders', () => {
    expect(renderWithProviders).toBeDefined();
  });
});
