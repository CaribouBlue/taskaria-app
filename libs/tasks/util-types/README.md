# libs/tasks/util-types

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build libs/tasks/util-types` to build the library.

## Running unit tests

Run `nx test libs/tasks/util-types` to execute the unit tests via [Jest](https://jestjs.io).
